server {
    listen 80 default_server;
    listen [::]:80 default_server;
    root /opt/static_site;
    server_name _;
    location / {
        try_files $uri $uri/ =404;
    }

    index index.html index.htm;

    location /files {
        autoindex on;
    }
}